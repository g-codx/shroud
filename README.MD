# Primitive VPN server and client

# Run

sudo required

[Server](./server)

``cargo build --release && sudo ./target/release/server``

[Client](./client)

``cargo build --release && sudo ./target/release/client``